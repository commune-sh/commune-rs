[package]
name = "test"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "test"
path = "src/lib.rs"

[dependencies]
dotenv = "0.15.0"
tokio = { version = "1.34.0", features = ["rt", "rt-multi-thread", "macros"] }

# Workspace Dependencies
serde = { workspace = true }
url = { workspace = true }

# Local Dependencies
matrix = { path = "../matrix" }
